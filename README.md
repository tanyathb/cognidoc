# cognidoc
Asynchronous, event-driven document processing and RAG pipeline. Ingests files via Blob Storage, queues tasks over Service Bus, generates embeddings with Azure OpenAI, indexes chunks in Azure AI Search (HNSW), and persists metadata in Cosmos DB with Polly rate-limit handling.
